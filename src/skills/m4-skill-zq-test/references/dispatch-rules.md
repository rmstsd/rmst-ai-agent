# M4 调度规则

- 当 `expectedRobotNames` 为空时，由后端而不是 Skill 选择机器人。
- 指定机器人或机器人组是分配约束，不保证执行一定成功。
- 后端分配会考虑订单资格、机器人资格、移动成本、优先级和创建时间。
- 机器人步骤选择是分配之后的独立阶段。
- 只有当订单状态为 `Executing` 时，才能声称订单正在执行。
- 只有当订单状态为 `Done` 时，才能声称订单已完成。
- 如果当前没有可用机器人，订单可能保持 `ToBeAllocated`；这不一定表示创建失败。
- 在声明可用性、调度或执行情况前，检查当前场景和机器人运行时状态。

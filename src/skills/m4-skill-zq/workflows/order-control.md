# 控制订单

在取消、暂停或恢复之前：

1. 解析订单 ID。如果只提供了描述，则查询订单并请求操作员消除歧义。
2. 查询当前订单详情。
3. 说明当前状态、已分配的机器人和故障状态。
4. 展示准确的修改操作并请求明确确认。
5. 仅在确认后调用相应 API。
6. 报告后端结果，并在需要时重新查询状态。

在后端确认且订单达到 `Cancelled` 状态之前，绝不要将取消请求描述为已完成。
